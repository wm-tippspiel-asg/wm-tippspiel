-- Run this ONCE after initial setup to create the admin account.
-- Replace the password hash with one generated by the setup script.
-- See docs/DEPLOYMENT.md for instructions.

-- INSERT OR IGNORE INTO users (id, username, password_hash, role)
-- VALUES (
--   lower(hex(randomblob(16))),
--   'admin',
--   'REPLACE_WITH_HASH_FROM_SETUP_SCRIPT',
--   'admin'
-- );
